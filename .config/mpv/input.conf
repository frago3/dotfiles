RIGHT                   seek  5
LEFT                    seek -5
Shift+RIGHT             no-osd seek  1 exact
Shift+LEFT              no-osd seek -1 exact 

UP                      add volume 1
DOWN                    add volume -1
WHEEL_UP                add volume -0.2
WHEEL_DOWN              add volume 0.2
WHEEL_LEFT              ignore
WHEEL_RIGHT             ignore

KP_HOME                 seek 0 absolute
# KP_END
# KP_PGUP
# KP_PGDWN
